define([
	'backbone'
],
function( Backbone ) {
    'use strict';

    /**
     * DestinationModel
     *
     * @return {Backbone.Model}
     */
	return Backbone.Model.extend({
		initialize: function() {
		},

		defaults: {
		},

    });
});
